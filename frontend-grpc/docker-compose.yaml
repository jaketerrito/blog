services:
  development:
    build: 
      context: ./
      dockerfile: Dockerfile
      target: development
    environment:
      - POSTS_API_URL=http://posts-grpc:50051
      - DEV_USER=local@test.com
      - AUTH_SESSION_SECRET=OGaEIw8Ee75cNguIvGZVE9aKuO9NINJP
    ports:
      - "5173:5173"
      - "3000:3000"
    volumes:
      - ./:/frontend
      - node_modules:/frontend/node_modules
    networks:
      - blog_network
volumes:
  node_modules:

networks:
  blog_network:
    name: blog_network
